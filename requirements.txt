# Pins realigned 2026-08-31 to the versions the code is developed and tested
# against. Before that, 14 of 18 were violated by the working venv, and the
# pinned fastapi~=0.115.0 made POST /api/crawl/start return 422 -- under 0.115,
# `from __future__ import annotations` leaves `background_tasks:
# BackgroundTasks` an unresolvable string and FastAPI treats it as a required
# query parameter. Verified green together on Python 3.11 (the Dockerfile pin)
# and 3.14 (development).
#
# tests/test_declared_environment.py fails if the installed set stops
# satisfying this file. Update the pin when you upgrade — do not silence it.

# Web framework
# v2.3 (M0.8 P7) — pinned to compatible-release (~=) so cold container builds
# can't surprise us by picking up an incompatible major. fastapi/pydantic/httpx
# in particular have had breaking changes between minors; the ~= operator
# allows patch updates only.
fastapi~=0.136.0
# Declared explicitly rather than relied on transitively via fastapi:
# every api/models module imports it at module level.
pydantic~=2.7
uvicorn[standard]~=0.46.0

# HTTP client — also used as async test client via httpx.AsyncClient
httpx~=0.28.0

# HTML parsing
beautifulsoup4~=4.14.0
lxml~=6.1.0

# Logging
python-json-logger~=4.1.0

# Rate limiting
slowapi~=0.1.9

# Async SQLite (dev)
aiosqlite~=0.22.0

# Environment variables
python-dotenv~=1.2.0

# PDF parsing
pypdf~=6.10.0

# Image processing
Pillow~=12.2.0
piexif~=1.1.3
python-multipart~=0.0.9

# PDF generation
fpdf2~=2.8.0

# Excel generation
openpyxl~=3.1.2

# Testing
pytest~=9.0.0
pytest-asyncio~=1.3.0
respx~=0.23.0
