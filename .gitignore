# Python
venv/
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
*.so
*.pyd
.mypy_cache/
.ruff_cache/

# Environment
.env
.env.local
*.env.local
.env-ttoad

# Database
*.sqlite
*.sqlite3
crawldb.sqlite

# Testing
.pytest_cache/
.coverage
htmlcov/

# Node / Frontend
node_modules/
frontend/dist/
frontend/.env.local
*.log

# Vercel
.vercel/

# macOS
.DS_Store

# WordPress credentials (never commit)
wp-credentials.json
talkingtoad.db
# Any copy of the database is the database (CLAUDE.md: a new file in this class
# is gitignored in the same change that introduces it). Covers the backups
# scripts/migrate_collapse_duplicate_origin_rows.py writes before it edits rows.
talkingtoad.db.*
*.db.bak
*.bak

# GEO configuration (domain-specific settings)
geo_settings.json
archive/
docs/.compiler-backups/
scratch/

# Google OAuth client-secret files — NEVER commit
client_secret*.json
*-secret*.json

# Tooling / orchestrator artifacts
.claude/
.orche/
.bridge_snapshot_marker
run-bridge.sh
repo_map.md
.aider*

# Generated advisor/audit report artifacts (keep binaries out of git history)
Advisor-Report_*.pdf

# Playwright (Phase 3 E2E)
frontend/playwright-report/
frontend/test-results/
