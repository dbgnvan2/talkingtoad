"""HTTP request/response Pydantic schemas (v2.6 M2.6 / Cycle EE).

Separate from `api/models/` which holds internal dataclasses (Issue,
CrawledPage, etc.). Anything that crosses the HTTP boundary lives here.
"""
