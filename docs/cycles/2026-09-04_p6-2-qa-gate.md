# QA Gate — P6.2: `rechecked` answers a question nobody asks it

**Gate date:** 2026-09-04
**Range:** `3338f84..61b5c1a` (`origin/main..HEAD`, 2 commits, clean tree)
**Gate type:** independent (Hermes, fresh context — the skill's standing rule that no agent verifies its own work)
**Verdict:** APPROVED — no blocking findings. Deletion is safe: the information is derivable everywhere it mattered, nothing consumed the field, and the wrong-target guard is real.

## Paste-ready verdict block for Claude Code

```
QA GATE — P6.2 (3338f84..61b5c1a): APPROVED, no blocking findings.

Evidence (all re-run on HEAD, not taken from the commit message):
- backend:  ./venv/bin/python -m pytest tests/ -p no:cacheprovider -q
            -> 5122 passed, 1 skipped, 2 deselected, 161.93s
- frontend: npx vitest run -> 392 passed / 49 files
- eslint:   nothing to lint — the cycle changed no .jsx/.js (crawl.py,
            two docs, one test .py only)
- static scan over added lines: no secrets, shell=True, eval/exec, pickle,
  or SQL f-strings

Deletion-safety checks (the point of this gate), all verified:
- No consumer ever read the boolean. grep frontend/src (outside __tests__)
  = exactly one hit, FixFocusPanel.jsx:108, and it reads the STRING form
  (=== 'not_checked') from the Fix Focus payload. RecheckResultBanner
  (Results.jsx:352) renders only the four code lists + checks_not_run_reason;
  toRecheckResult (Results.jsx:434) is a pass-through spread. The by_category
  read at Results.jsx:786 is the /pages/issues Page Audit payload, which
  never carried the field. Backend: zero readers, zero producers remain
  (only the P6.2 comment at crawl.py:1925).
- The information is derivable on every row, always. Carry-over is defined
  as code-in-needs_full_crawl (crawl.py:1877-1882, registry-derived), and an
  unrunnable code cannot be newly found, so rechecked == (code not in
  needs_full_crawl). Pinned by 4.3 through SQLite, not string comparison.
- The Fix Focus string form is untouched and genuinely live: deleting
  item["rechecked"] = "not_checked" from fix_focus.py:210 turns BOTH the new
  4.4 guard AND the pre-existing test_fix_focus.py::test_u45_* red
  (gate-verified by mutation, then restored).
- Doc lines gone from docs/api.md:47 and functional spec; the spec's
  historical note is prose ("a per-row `rechecked` boolean"), which 4.5's
  `rechecked: true|false` contract regex correctly ignores.

Spec table 4.1-4.5: all five rows have real tests in
tests/test_rescan_reports_what_it_checked.py. 4.1 keeps its true assertion
(presence) and loses the fabricated-consumer claim; 4.2 = the field is gone
and must not return — gate-verified red by re-adding the field to the
new_issues merge; 4.3 pins the deletion's premise over the whole grid
(stored_before ∩ unrunnable, seeded with a runnable code that must NOT be
carried); 4.4 guards the wrong target (the string Fix Focus field, verified
red by mutation); 4.5 reads the docs and asserts against the LIVE response
per LEARNINGS item 13 — not two assertions agreeing with each other.

One non-blocking finding (the P5.3 shape, recurring — handle at fold):
the fix commit message says "recorded as TODO P6.2b rather than closed
over", but P6.2b exists ONLY in docs/pending/2026-09-04_p6-2-*.md — no
TODO.md entry, no LEARNINGS mention. Same false "recorded in TODO" claim
the P5.3 gate caught (and LEARNINGS documents). The record is safe only
while the pending file survives; the fold MUST transcribe P6.2b into
TODO.md BEFORE deleting the pending file, or the gap is lost with it.

Fold checklist (same shape as the P6.1 fold 340005e):
- transcribe P6.2b (persistent drawer gap, needs stored per-row state +
  schema change -> own item) into TODO.md, THEN delete
  docs/pending/2026-09-04_p6-2-a-field-that-answers-a-question-nobody-asks-it.md
- check off TODO.md P6.2 (line 201; Phase 6 header becomes "3 — 2 done")
- optional: LEARNINGS fix-log entry — this was hygiene (dead field), not a
  user-facing bug; P6.1 earned an entry because a wrong answer shipped.
  If you add one, the pattern is P25/P26/P13 + the recurring false
  "recorded in TODO" claim above.
- commit this gate file (docs/cycles/2026-09-04_p6-2-qa-gate.md) with the fold
- git push origin main
```

## Evidence

| Check | Command | Result |
|---|---|---|
| Range/state | `git log origin/main..HEAD` + `git status` | exactly 3338f84 (spec) + 61b5c1a (fix); clean tree; ahead of origin by the 2 cycle commits only |
| Backend suite | `./venv/bin/python -m pytest tests/ -p no:cacheprovider --tb=short -q` | **5122 passed, 1 skipped, 2 deselected** (integration), 161.93s — matches the commit's "5122 backend" |
| Frontend suite | `npx vitest run` (frontend/) | **392 passed / 49 files** — matches the commit's "392 frontend" |
| Lint | — | no .jsx/.js changed in the cycle; nothing to lint |
| Static scan | added lines grepped for secret/token/password, `os.system`/`shell=True`, `eval(`/`exec(`, `pickle`, SQL-f-string | clean (no matches) |
| Mutation 1 (4.2 guard) | re-added `\| {"rechecked": True}` to the new_issues merge in crawl.py | `test_the_rescan_response_carries_no_unread_rechecked_flag` RED; 4.3 and 4.5 stayed green (correct — they guard other premises); restored, tree clean |
| Mutation 2 (4.4 guard) | deleted `item["rechecked"] = "not_checked"` from fix_focus.py:210 | new 4.4 guard RED **and** pre-existing `test_fix_focus.py::test_u45_checked_item_keeps_its_tick_when_not_checked` RED — the string field is genuinely live; restored, tree clean, 37 passed on re-run |

## Diff vs micro-spec (`docs/pending/2026-09-04_p6-2-...md`)

| Spec § | Change | Implementation | Test |
|---|---|---|---|
| 3.1 | Delete `rechecked` from both `_issue_dict` merges + the carried-over branch | `crawl.py:1828` (unreadable branch), `crawl.py:1921` (new_issues), `crawl.py:1932` (carried_over) — all three `_issue_dict(i) \| {...}` merges now plain `_issue_dict(i)` | 4.2 (no row carries the flag); verified red by gate mutation |
| 3.2 | Delete the doc lines in `docs/api.md:47` and the functional spec | both edited; spec gains a dated historical parenthetical, not a contract line | 4.5 scans both docs for `` `rechecked: true\|false` `` and asserts against the live response |
| 3.3 | Fix the test: keep presence, drop the fabricated-consumer claim | `test_a_carried_over_finding_is_still_in_the_response` — P6.2 note in the docstring explains what was dropped and why | 4.1 (still asserts every unrunnable code is present) |
| 3.4 | Guard against the field returning | `test_the_rescan_response_carries_no_unread_rechecked_flag` | 4.2 — mutation-verified red |
| — | §4.3 premise pin (carry-over == stored ∩ needs_full_crawl) | seeds runnable + unrunnable codes, asserts exact equality through the real endpoint | 4.3 |
| — | §4.4 wrong-target guard | `test_fix_focus_rechecked_is_untouched` builds a snapshot, applies verify, asserts the string form | 4.4 — mutation-verified red, and the pre-existing fix_focus suite independently pins it |
| — | §4.5 docs-vs-live | reads both docs for the contract regex, asserts the live response lacks the field | 4.5 |

Scope additions beyond the spec: none. §2's P6.2b (persistent drawer gap) is
recorded in the spec as out-of-scope — see the fold finding below.

## Commit-message claims spot-checked

- "No consumer ever read it: grep frontend/src outside __tests__ returns
  only the Fix Focus hit" — confirmed: FixFocusPanel.jsx:108 reads the
  STRING (`=== 'not_checked'`), a different field on a different payload.
  The rescan response reaches the UI via toRecheckResult →
  RecheckResultBanner, which renders code lists only and never per-issue
  rows (Results.jsx:352-432); toRecheckResult (434-437) is a spread, so no
  row-level field is lifted out anywhere.
- "It was derivable on every row, always" — confirmed structurally:
  `carried_over = [issue ... if issue.issue_code in unrunnable]`
  (crawl.py:1877-1882), unrunnable = registry-derived
  `_checks_a_single_page_scan_cannot_run()`, and the unreadable-branch
  carry-over is `old_codes & unrunnable` (crawl.py:1824-1825). Test 4.3
  pins the equivalence through SQLite.
- "4 mutations verified red, including the wrong-target grep sweep" —
  two independently re-run by the gate (see Evidence); both red exactly as
  claimed. The other two (field returns in carried-over branch; doc line
  restored) are covered by 4.2/4.5 asserting the wrong behaviour they kill.
- "recorded as TODO P6.2b rather than closed over" — **NOT confirmed**:
  P6.2b appears nowhere in TODO.md (only in the pending file, lines 68/121/130).
  This is the same false "recorded in TODO" claim the P5.3 gate caught.
  Non-blocking for the code; the fold must transcribe it before deleting
  the pending file (see below).

## Static scan

No hardcoded secrets, no `os.system`/`shell=True`, no `eval`/`exec`, no
`pickle`, no SQL built from f-strings in the cycle's added lines.

## Non-blocking observations

1. **"Recorded as TODO P6.2b" is false at HEAD (recurring P5.3 shape).** The
   fix commit message claims the persistent-drawer gap was "recorded as
   TODO P6.2b". It lives only in the pending spec file. TODO.md:201 still
   shows P6.2 unchecked and nothing named P6.2b exists. The irony is the
   cycle's own subject: a past-tense claim ("so the panel cannot
   distinguish…") that became the record of a dependency that never held —
   and the fix's message makes the same shape of claim about its own
   bookkeeping. LEARNINGS already documents this exact catch from the P5.3
   gate ("I wrote 'recorded in TODO' for an observation I never recorded").
   Consequence if ignored: the fold step deletes the pending file and the
   P6.2b record vanishes with it. Transcribe first.
2. **The fold into the functional spec is a prose parenthetical, not a
   blockquote.** P6.1's fold added a dated blockquote; P6.2's fix edited the
   D5 paragraph inline. Both record the change; the inline form is fine for
   a deletion whose behavioural sentence is the surrounding paragraph.
   Consistency note only.
3. **No LEARNINGS fix-log entry for this cycle.** Defensible — this deleted a
   dead field and corrected a lying test message; no wrong answer ever
   shipped on its account. The P25/P26/P13 pattern vocabulary is already in
   the index. Optional at fold; not required.

## State (expected pre-fold, matching the P6.1 pattern)

- `docs/pending/2026-09-04_p6-2-a-field-that-answers-a-question-nobody-asks-it.md`
  still present — and it is currently the ONLY home of the P6.2b record.
- `TODO.md:201` P6.2 still `- [ ]`; Phase 6 header still "3 — 1 done".
- No LEARNINGS.md fix-log entry for P6.2.
- `origin/main..HEAD` = the 2 cycle commits (spec + fix), unpushed.
- All of the above resolve at the fold step, which historically (340005e)
  also commits this gate file and pushes. The fold MUST carry the P6.2b
  transcription (finding 1) — that is the one amendment this gate requires.

**Verdict: APPROVED.**
