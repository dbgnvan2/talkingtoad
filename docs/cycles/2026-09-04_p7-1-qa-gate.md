# QA Gate — P7.1: the four tools that change a site now explain themselves

**Gate date:** 2026-09-04
**Range:** `a84010f..f0a89c2` (`origin/main..HEAD`, 2 commits, clean tree)
**Gate type:** independent (Hermes, fresh context — the skill's standing rule that no agent verifies its own work)
**Verdict:** APPROVED — no blocking findings. Every `misleading` field names a real, code-verified failure mode of its own tool; the batch-optimise caveat is accurate against both the implementation and CLAUDE.md's WordPress constraints; all claims in the commit message reproduce under re-run.

## Paste-ready verdict block for Claude Code

```
QA GATE — P7.1 (a84010f..f0a89c2): APPROVED, no blocking findings.

Evidence (all re-run on HEAD, not taken from the commit message):
- backend:  ./venv/bin/python -m pytest tests/ -p no:cacheprovider -q
            -> 5162 passed, 1 skipped, 2 deselected, 157.60s
            (matches the commit's "5162 backend")
- frontend: npx vitest run -> 396 passed / 50 files
            (392 pre-existing + this cycle's 4 new — matches "396 frontend")
- new pytest file alone: tests/test_panel_help_completeness.py
            -> 27 passed (matches the commit's "27 pytest")
- eslint on all 10 changed .jsx: 0 errors; 8 warnings, all pre-existing
  (unused React imports/vars on lines this cycle did not touch)
- static scan over added lines: no secrets, shell=True, eval/exec,
  pickle, or SQL f-strings; no dead refs to the removed V4/V4_EXPLAINER
  consts

Every misleading field was fact-checked against the implementation
(not just against the spec) — all nine name a real failure mode:
- faq-schema: refusal semantics verified at faq_schema_generator.py:59-70
  (refuses when <2 usable pairs, JS-only reason text) + ai.py:683-714
  (copy/export only, never writes WP — module docstring line 9-10)
- geo-settings: unverified inputs feeding alt-text + GPS EXIF verified at
  wp_image_fixer.py:345-359 (GPS injection per optimised file) + 379-417
  (geo-config-driven AI alt text on the NEW media item); GeoConfig is
  stored as typed in, no validation against any register
- image-analysis: score = metadata/file weight only verified at
  image_analyzer.py:149-152 (is_decorative skips IMG_ALT_MISSING — a
  decorative spacer with a tidy filename genuinely scores well);
  150 cap verified at engine.py:92/1352 + thresholds.md:249 (already
  canonical); data_source per row at engine.py:1462-1467 and rendered
  "partial" in ImageAnalysisPanel.jsx:655-656
- batch-optimise: uploads NEW files / pages keep the old / manual swap
  verified at wp_image_fixer.py:230-234 (Workflow A docstring), 369-377
  ("upload to WordPress (NEW file, don't delete old)"), 424-431 (message:
  "Replace on: ..." / "Link it to your pages manually"); replace_link_in_post
  is imported at :266 but never called in the flow. Matches CLAUDE.md
  ("DO NOT AUTOMATE IMAGE LINK UPDATES IN POSTS/PAGES")
- gsc-insights: lag/empty-panel + position-is-an-average clauses are GSC
  data semantics, not this codebase's invention; panel shows Google's
  per-page averages
- geo-faq: generated from GeoConfig topics/locations only (geo_faq.py),
  never re-fetches the page -> "nothing checked against what a visitor
  sees" is true; the contrast with the per-page tool is accurate
- entity-schema: sameAs taken verbatim from entity_wikipedia_url with no
  verification (geo_schema_factory.py:35-36)
- page-priority / fix-focus: migrated with light polish; fix-focus's
  "24 checks that need a full crawl" verified — registry counts exactly
  24 needs_full_crawl=True codes and apply_verify (fix_focus.py:204-211,
  U4.5/P31) marks them "not re-checked" rather than verified

Spec table 3.1-3.6: all six rows have real tests in
tests/test_panel_help_completeness.py; 3.3+3.4 are a proven pair (four
vapid samples asserted rejected; weakening the check to len(text)>0
fails 3.4 by construction); 3.5 is the P13 guard over the whole src tree;
3.6 reads the ids out of the JSX. Extras beyond the table: 3.3b
(no-restatement) and 3.5b (labels defined once in the component).

Non-blocking (handle at fold):
1. Spec's vitest row promises "each of the four renders its explainer
   behind the toggle" — vitest mounts only BatchOptimizePanel behind its
   toggle (the specific-sentence test). FaqSchemaModal, GeoSettingsModal
   and ImageAnalysisPanel toggles are covered only by pytest 3.6 (their
   ids are registered and their JSX is the same copy-pasted toggle
   pattern). Either extend PanelExplainer.test.jsx or amend the spec row
   at fold to what is pinned.
2. "150" is hardcoded as user-facing prose in image-analysis's misleading
   (TT_IMAGE_DIMENSION_MAX_COUNT default; thresholds.md:249 owns it and
   the env can override it). The env name is cited in the same sentence,
   which is the mitigation. Keep as-is or reword without the digit
   (note: the substance check needs a digit/caps token in every
   misleading, so a rewrite must keep one).
3. test 3.6 asserts rendered-ids ⊆ PANEL_HELP but not the reverse — an
   orphan entry no panel renders would pass every test silently
   (currently 9/9 are rendered; no live failure). One-line addition:
   assert not (set(PANEL_HELP) - used).
4. Migrated entries' misleading fields were sharpened, not verbatim:
   the old inline geo-faq/entity-schema copy would have FAILED the new
   substance check (no digit/ALL-CAPS token), so the rewrites were
   required for the migration to be honest. All sharpened clauses
   verified code-accurate (above). Say so in one sentence at the fold.
5. PLAN-V4.0.md running tally (line ~107) has no P7.1 row yet — add one
   (this cycle shipped V4 explainers for four tools); TODO.md:257-259
   Phase 7 item still unchecked.

Fold checklist:
- fold docs/pending/2026-09-04_p7-1-panel-explainers.md into
  docs/functional-specification.md (note finding 1's wording and finding
  4's scope sentence), THEN delete the pending file
- TODO.md:257 Phase 7 header -> "1 — 1 done", check off P7.1
- add the P7.1 row to PLAN-V4.0.md's running tally (finding 5)
- commit this gate file with the fold, then git push origin main
```

## Evidence

| Check | Command | Result |
|---|---|---|
| Range/state | `git log origin/main..HEAD` + `git status` | exactly a84010f (spec) + f0a89c2 (feat); clean tree; ahead of origin by the 2 cycle commits only |
| Backend suite | `./venv/bin/python -m pytest tests/ -p no:cacheprovider --tb=short -q` | **5162 passed, 1 skipped, 2 deselected** (integration), 157.60s — matches the commit's "5162 backend" |
| New pytest file | `./venv/bin/python -m pytest tests/test_panel_help_completeness.py -p no:cacheprovider -q` | **27 passed** — matches the commit's "27 pytest" (9×3.1 + 1×3.2 + 9×3.3 + 4×3.4 + 3.3b + 3.5 + 3.6 + 3.5b) |
| Frontend suite | `npx vitest run` (frontend/) | **396 passed / 50 files** — 392 pre-existing + 4 new (PanelExplainer.test.jsx) |
| Lint | `npx eslint <10 changed .jsx files>` | **0 errors**; 8 warnings, all on pre-existing lines (GEOReportPanel.jsx:1/200, ImageAnalysisPanel.jsx:44/516/531/652/1263, PagePriorityPanel.jsx:2 — unused React import/vars untouched by the cycle). New files PanelExplainer.jsx + test clean |
| Static scan | added lines grepped for secret/token/password, `os.system`/`shell=True`, `eval(`/`exec(`, `pickle`, SQL f-strings; dead refs to removed `V4`/`V4_EXPLAINER` consts | clean (no matches) |
| Copy fact-check | misleading fields read against engine.py / wp_image_fixer.py / faq_schema_generator.py / geo_schema_factory.py / geo_faq.py / image_analyzer.py / registry.py | all nine code-verified (details in verdict block) |

## Diff vs micro-spec (`docs/pending/2026-09-04_p7-1-panel-explainers.md`)

| Spec § | Change | Implementation | Test |
|---|---|---|---|
| 2.1 | One component + one data module; five labels once | `PanelExplainer.jsx` (labels inline, 5 `<strong>` tags, `className` pass-through) + `panelHelp.json` (9 entries × 5 parts). Note: spec said `panelHelp.js`; implementation is `panelHelp.json` (default-imported + re-exported as `PANEL_HELP`) — same single-home property, benign deviation | 3.5 (no hand-written labels in any other .jsx), 3.5b (component carries all five), 3.1 |
| 2.1 (migration) | Three existing copies migrate | Five migrated (structural sweep found PagePriorityPanel + FixFocusPanel the TODO had not listed — committed as a documented scope addition). GEOReportPanel's two sub-panels keep their `showHelp` toggles; GSCInsightsPanel keeps always-on rendering | 3.6 reads all rendered ids (9) |
| 2.2 | The four panels get entries + "What is this?" toggle | FaqSchemaModal / GeoSettingsModal / ImageAnalysisPanel / BatchOptimizePanel each render `<PanelExplainer>` behind a `showHelp` toggle | vitest (BatchOptimizePanel behind toggle; see finding 1), 3.6 |
| 2.3 | Copy with real failure modes | All four written as spec'd; substance verified against implementation (verdict block) | 3.3, 3.3b, 3.4 (adversarial) |
| 3 | Test table | 3.1–3.6 all present with real tests; vitest row implemented for BatchOptimizePanel's specific sentence (see finding 1 for the "each of the four" gap) | 27 pytest + 4 vitest |

Scope additions beyond the spec, all documented in the commit and code-verified:
- **Two extra hand-written copies found and migrated** (PagePriorityPanel, FixFocusPanel) — five, not three; their labels had already drifted ("Why it matters"/"Good vs. bad"/"How to use it" vs the others). The migration unified the labels, which is the exact divergence 2.1 exists to prevent.
- **Misleading fields on migrated entries sharpened** (geo-faq, entity-schema, gsc-insights, fix-focus) — the old inline geo-faq/entity-schema caveats fail the new substance check, so the rewrites were mandatory for migration; every added clause is code-verified (finding 4).
- **`panelHelp.json` over spec'd `panelHelp.js`** — format choice, same one-home semantics.
- The four target panels' toggles use the identical "What is this?" / "Hide" markup — same affordance shape GEOReportPanel already used.

## Commit-message claims spot-checked

- "5162 backend / 396 frontend, build clean" — confirmed: 5162 passed / 396 passed; eslint 0 errors.
- "the structural guard then found two the TODO had not listed — five copies, and their labels had already drifted" — confirmed in the diff (PagePriorityPanel/FixFocusPanel const blocks removed, label text differs from the others' in the removed lines) and by the vitest file's header comment.
- "27 pytest (completeness, substance, no-restatement, one-home, id-validity) + 4 vitest" — confirmed: file runs 27 passed; test file has 4 `it()` blocks.
- "Six mutations verified red, including ... a vapid caveat, and the substance check itself being weakened to `len(text) > 0`" — not re-run (mutation of the suite's own code), but both named cases are red by construction: 3.4's four vapid samples would pass a `len(text) > 0` check and fail the `assert is False`; the pair is genuine.
- "It is the single most important line in this item, and the vitest asserts that sentence rather than 'an explainer rendered'" — confirmed: the BatchOptimizePanel test asserts `/uploads NEW files/i` and `/keep serving the old images until you replace each one by hand/i` against the rendered block.

## Static scan

No hardcoded secrets, no `os.system`/`shell=True`, no `eval`/`exec`, no `pickle`, no SQL built from f-strings in the cycle's added lines. No dead references to the removed `V4`/`V4_EXPLAINER` objects anywhere in `frontend/src/components/` or `frontend/src/data/`.

## Non-blocking observations

1. **Vitest spec-row gap (the "each of the four" promise).** Spec §3's vitest row says "each of the four renders its explainer behind the toggle | §2.2 regresses". The vitest file mounts only BatchOptimizePanel behind its toggle; FaqSchemaModal, GeoSettingsModal and ImageAnalysisPanel are never mounted, so their toggle affordance is asserted nowhere in vitest. The risk surface is small — the toggle markup is byte-identical across the four, and pytest 3.6 proves each rendered id resolves in `PANEL_HELP` — but the spec promised each of the four. Either extend `PanelExplainer.test.jsx` (mount GeoSettingsModal/FaqSchemaModal behind their toggles — both are self-contained) or amend the spec row at the fold to describe what is actually pinned.
2. **`150` as prose in the data.** The image-analysis misleading hardcodes the `TT_IMAGE_DIMENSION_MAX_COUNT` default (150) as user-facing copy. `docs/thresholds.md:249` owns the number and the code reads it from an env override (`engine.py:92`), so a deployment that sets the env would render the explainer stale. The env name is cited in the same sentence, which is the mitigation; and the substance check (3.3) requires a digit/ALL-CAPS token in every misleading, so any rewrite must keep one. Cosmetic.
3. **No reverse-direction guard on the registry.** 3.6 asserts every rendered id exists in `PANEL_HELP` (used ⊆ registered) but not the converse — an orphan entry no panel renders would sail through 3.1/3.3/3.5/3.6 (3.1/3.3 parametrize over `PANEL_HELP`, so orphans get five parts and substance and still render nowhere). Currently 9/9 entries are rendered, so there is no live failure; a one-line `assert not (set(PANEL_HELP) - used)` in 3.6 would close it.
4. **Migrated copy was sharpened, not carried over.** The five migrated entries' misleading fields differ materially from the removed inline copy (e.g. entity-schema now names the unverified-`sameAs` mechanism; fix-focus now names the 24 full-crawl checks; gsc-insights adds the position-is-an-average clause). This is the substance check working as designed — the old geo-faq/entity-schema caveats would have failed 3.3 — and every new clause verified code-accurate, but spec §2.1's "migrate" reads as verbatim. One sentence in the fold commit resolves the record.
5. **PLAN-V4.0.md tally row due.** The running tally (PLAN-V4.0.md:107-130) ends at the O-series; P7.1 ships V4 explainers for four tools (and moves five existing ones under one home), which is exactly what the tally tracks. TODO.md:257-259 ("Phase 7 — the V4 second half (1)", P7.1 `- [ ]`) also still open. Both resolve at the fold.

## State (expected pre-fold, matching the P6.1–P6.3 pattern)

- `docs/pending/2026-09-04_p7-1-panel-explainers.md` still present — fold pending.
- `docs/functional-specification.md`: zero references to P7.1/PanelExplainer/panelHelp — fold pending.
- `docs/thresholds.md`: untouched in this range — correct; the cycle introduces no new numeric bounds (the 150 cap and the 24 full-crawl checks were already canonical at thresholds.md:249 and registry.py's `needs_full_crawl`).
- `TODO.md:257-259`: Phase 7 header "1", P7.1 still `- [ ]`.
- `PLAN-V4.0.md`: no P7.1 tally row yet.
- `origin/main..HEAD` = the 2 cycle commits (spec + feat), unpushed.
- All of the above resolve at the fold step, which historically (e0e9377, e4843be, 340005e) also commits this gate file and pushes. No gate-required amendments this time — findings 1–5 are record-and-decide, not fix-before-merge.

**Verdict: APPROVED.**
