# QA Gate — P6.1: a single-page scan scores 100 and calls itself comparable

**Gate date:** 2026-09-04
**Range:** `f75385a..144c37e` (`origin/main..HEAD`, 2 commits, clean tree)
**Gate type:** independent (Hermes, fresh context — the skill's standing rule that no agent verifies its own work)
**Verdict:** APPROVED

## Paste-ready verdict block for Claude Code

```
QA GATE — P6.1 (f75385a..144c37e): APPROVED, no blocking findings.

Evidence (all re-run on HEAD, not taken from the commit message):
- backend:  ./venv/bin/python -m pytest tests/ -p no:cacheprovider -q
            -> 5116 passed, 1 skipped, 2 deselected, 182s
- frontend: npx vitest run -> 392 passed / 49 files
- eslint on SummaryPanel.jsx + SummaryPanelSinglePage.test.jsx -> 0 errors
  (only the known pre-existing SummaryPanel.jsx:1 unused-React warning)
- static scan over added lines: no secrets, shell=True, eval/exec, pickle,
  or SQL f-strings

Spec table: all seven rows (3.1-3.7) have real tests in
tests/test_single_page_scope_disclosure.py, each asserting the exact wrong
fix it guards (3.2: key ABSENT not []; 3.3: registry computed in the test
via patch.dict, so a hand-kept list cannot satisfy it; 3.4: delta present,
labelled not withheld; 3.5: same-page rescans stay comparable); plus a bonus
3.7b pinning that `comparable` kept its category meaning. §2.4 has 4 vitest
tests incl. the adversarial toBeVisible (not toBeInTheDocument) assertion.

One non-blocking finding (P16/P24 family — do NOT let it block the fold):
get_summary's checks_not_run_reason is a NEW string written inline in
sqlite_store.py, duplicating _CHECKS_NOT_RUN_REASON (crawl.py:485) with
different wording, and the code list has a second derivation expression
inline in the store beside _checks_a_single_page_scan_cannot_run()
(crawl.py:626). Both lists are individually registry-pinned by tests, so
nothing can drift silently; the reason phrase is not pinned anywhere.
Root cause is layering: the helper+constant live in the router, and the
store cannot import them. Optional clean-up: move both into a shared home
(registry.py or the issue_checker facade) and delete the store's inline
copies. If you leave it, leave a comment pointing the two phrasings at each
other.

Fold checklist (same shape as the P5.4 fold commit 1f4ebaa):
- fold spec into docs/functional-specification.md and DELETE
  docs/pending/2026-09-04_p6-1-a-single-page-scan-scores-100.md
- add the P6.1 fix-log entry to LEARNINGS.md (P25/P31/P12) as prior folds did
- commit this gate file (docs/cycles/2026-09-04_p6-1-qa-gate.md) with the fold
- TODO.md P6.1: mark done at fold (currently `- [ ]` at line 184)
- git push origin main
```

## Evidence

| Check | Command | Result |
|---|---|---|
| Range/state | `git log origin/main..HEAD` + `git status` | exactly f75385a (spec) + 144c37e (fix); clean tree; ahead of origin by the 2 cycle commits only |
| Backend suite | `./venv/bin/python -m pytest tests/ -p no:cacheprovider --tb=short -q` | **5116 passed, 1 skipped, 2 deselected** (integration), 182s — matches the commit's "5116 backend" |
| Frontend suite | `npx vitest run` | **392 passed / 49 files** — matches the commit's "392 frontend" |
| Lint | `npx eslint src/components/SummaryPanel.jsx src/components/__tests__/SummaryPanelSinglePage.test.jsx` | 0 errors; only the known pre-existing `SummaryPanel.jsx:1` unused-React warning (non-blocking) |
| Static scan | added lines grepped for secret/token/password, `os.system`/`shell=True`, `eval(`/`exec(`, `pickle`, SQL-f-string | clean (exit 1 = no matches) |

## Diff vs micro-spec (`docs/pending/2026-09-04_p6-1-...md`)

| Spec § | Change | Implementation | Test |
|---|---|---|---|
| 2.1 | `page_scope` + `pages_scored` on `health_score_basis`, `comparable` unchanged | `job_store_base.py:health_score_basis(..., pages_crawled)`; both `mode=all` branches and the partial branch carry `**scope` | 3.7 parametrized both directions (single_page/1, site/10); 3.7b pins `comparable is True` + category meaning on a single-page job |
| 2.2 | summary gains `checks_not_run` + reason, single-page ONLY, ABSENT (not `[]`) on full crawl, registry-derived | `sqlite_store.py:get_summary` — key added only when `settings.single_page`, derived from `_CATALOGUE` `needs_full_crawl` | 3.1 (names all 24, reason non-empty), 3.2 (key absent on full crawl), 3.3 (patch.dict on `_CATALOGUE` moves the list — hand-kept copy cannot satisfy) |
| 2.3 | 4th comparison reason: scope mismatch; same-scope different-URL refused; same-URL rescans stay comparable; delta still returned | `crawl.py:get_crawl_comparison` — scope pair read off both summaries' basis with `"site"` fallback | 3.4 (comparable False + reason + `delta is not None`), 3.5 (same page stays comparable — the wrong-fix guard), 3.6 (different pages refused) |
| 2.4 | SummaryPanel banner: count in sentence, names behind a disclosure, absent on full crawl | `SummaryPanel.jsx` — blue banner gated on `checks_not_run.length > 0`, `<details>` disclosure | 4 vitest tests: count stated (9 of the 24-code fixture, list collapsed → `not.toBeVisible()`), names on demand, absent on full crawl, legacy summary unchanged |

Scope additions beyond the spec: none — 3.7b is an extra pin, documented in its
docstring, guarding the spec's own "comparable keeps its meaning" sentence.

## Commit-message claims spot-checked

- "24 catalogue codes carry needs_full_crawl" — confirmed: 24 in the registry
  (`needs_full_crawl=True` sites), asserted by 3.1's `len == 24` premise guard.
- "checks_not_run is returned by four endpoints and rendered in exactly one
  place" — confirmed at `crawl.py:1839/1975/2200/2478`, all sharing
  `_checks_a_single_page_scan_cannot_run()` (crawl.py:626) and
  `_CHECKS_NOT_RUN_REASON` (crawl.py:485); pre-existing sites, unchanged.
- The four endpoints' list and the store's new list are the same comprehension
  over `_CATALOGUE.needs_full_crawl`; the endpoint helper is pinned by the
  pre-existing `tests/test_single_page_scan_discloses_inert_checks.py`, the
  store path by 3.3 — twin registry pins, so the two sites cannot drift
  silently.
- "9 mutations verified red" — not re-run (mutation runs are destructive by
  design); the surviving tests each assert the exact wrong-fix behaviour they
  claim to kill, which is the load-bearing part, and the full suite is green.

## Static scan

No hardcoded secrets, no `os.system`/`shell=True`, no `eval`/`exec`, no
`pickle`, no SQL built from f-strings in the cycle's added lines.

## Non-blocking observations

1. **Duplicated disclosure phrasing + duplicated derivation (P16/P24 family).**
   `sqlite_store.py` writes its own `checks_not_run_reason` ("These checks
   compare a page against the rest of the site…") while the established
   `_CHECKS_NOT_RUN_REASON` ("These checks only run during a full crawl… their
   absence is not a pass…") lives at `crawl.py:485` — two live user-facing
   sentences for one fact, and the code list is derived inline in the store
   beside the router helper. Both semantics are true and both lists are
   registry-pinned, so this is drift-risk, not a wrong result. Root cause is
   layering: the helper and constant live in the router and the store cannot
   import them without a cycle. Recommended shape (optional, do not hold the
   fold for it): move `_checks_a_single_page_scan_cannot_run()` and
   `_CHECKS_NOT_RUN_REASON` next to `_CATALOGUE` (registry.py or the
   `issue_checker` facade) and have both `crawl.py` and `sqlite_store.py`
   import them; delete the store's inline copies. This is the same
   shared-helper consolidation the P16/P24 history keeps converging on.
2. **ORPHAN_PAGE is double-disclosed** — it appears in the new summary list
   AND in `orphan_detection.status`. Consistent (same fact, two honest
   surfaces), not contradictory; noted for completeness only.
3. **The frontend fixture carries 9 of the 24 codes** and asserts "9 checks
   could not run". Deliberate (the count renders from `array.length`, so the
   fixture tests the mechanism, not the magic number); the backend 3.1 asserts
   the real 24 against the registry.

## State (expected pre-fold, matching the P5.4 pattern)

- `docs/pending/2026-09-04_p6-1-a-single-page-scan-scores-100.md` still
  present; `docs/functional-specification.md` has no P6.1 fold yet.
- `TODO.md:184` P6.1 still `- [ ]`; no LEARNINGS.md fix-log entry yet.
- `origin/main..HEAD` = the 2 cycle commits (spec + fix), unpushed.
- All of the above resolve at the fold step, which historically (1f4ebaa) also
  commits this gate file and pushes. No fold amendments are required by this
  gate.

**Verdict: APPROVED.**
