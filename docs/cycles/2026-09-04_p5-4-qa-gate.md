# QA Gate — P5.4 "prevalence judges an old job by today's catalogue"

**Date:** 2026-09-04 · **Reviewer:** Hermes (mini1) — independent QA gate, no context of authorship
**Cycle:** `46eaf9e` (P5.4 micro-spec) → `b72b72d` (P5.4 fix)
**Range:** `origin/main..HEAD` = `46eaf9e..b72b72d` (2 commits, both unpushed)
**Verdict:** ✅ **APPROVED** — no blocking findings. Fold-step items listed below are bookkeeping the disposal commit must carry, mirroring the P5.3 cycle (`d52415b`).

---

## Verdict for Claude Code (paste-ready)

> Independent QA gate on P5.4 (`46eaf9e` spec → `b72b72d` fix): **APPROVED**.
>
> Evidence, all re-run by the gate on HEAD (not inferred from commit messages):
> - Python suite: **5107 passed, 1 skipped, 2 deselected** (integration), 184.2s, `./venv/bin/python -m pytest tests/ -p no:cacheprovider`. Matches the commit's claim exactly.
> - Targeted: **64 passed** in `tests/test_prevalence_agreement.py` + `tests/test_prevalence.py`.
> - Frontend vitest: **388 passed / 48 files**, 5.3s (no frontend files changed in this cycle — `npx eslint` on changed files is vacuous; nothing jsx/js in the range).
> - Static scan of added lines over the range: clean (no secrets / os.system / shell=True / eval / exec / pickle / SQL-injection patterns).
> - Spec §3.1–3.4 implemented as written. `build_prevalence` now passes `(code, url, impact, severity, category)` (prevalence.py:231-235); `Prevalence` gains `impact`, and severity/impact come from the stored row (prevalence.py:44-48, 121-122, 140); `_prevalence_for_display` filters on `r.impact` with `derive_impact`'s import dropped (crawl.py:871-875); a catalogue-forgotten code keeps its row, and only a code the catalogue KNOWS can be declared site-scoped (prevalence.py:125-129); mixed stored impacts take the deterministic maximum (prevalence.py:118, 133-135).
> - Test table 4.1–4.7 all present with real bodies. **4.3 is the pin that matters**: parametrized over 4 `info_detail` levels × 5 stored impacts, it recalibrates `_IMPACT_OVERRIDES` to the far end of the band (0↔5, so a re-deriving surface cannot accidentally agree) and reads BOTH live surfaces — the `/results` endpoint and `_prevalence_for_display` — asserting they name the same code. It does not recompute the predicate on either side.
> - Independent mutation check by the gate (reverted clean, tree byte-identical): restoring the old `derive_impact(r.code)` filter in crawl.py made **12 tests fail** — 4.1, 4.2, and 12 discriminating 4.3 grid cells (the cells where the recalibration changes the predicate outcome). The tests pin exactly the defect class the item is about.
> - The e4_1b supersession is done properly: assertion inverted in place with the reason and live row counts (4,559 rows / six §7-deleted codes) in the docstring, "must not crash" half kept.
> - Scope additions are documented and tested: 4.5b (stored-category fallback, spec §3.1's last paragraph) and 4.5c (a KNOWN code also keeps its stored severity — the mutation-survivor the commit message names; `severity=spec.severity if spec else severity` is exactly the variant it goes red on).
>
> **Fold-step items (bookkeeping, nothing blocking — the P5.3 pattern):** fold the spec into `docs/functional-specification.md`, delete `docs/pending/2026-09-04_p5-4-prevalence-reads-todays-catalogue.md`, check off TODO P5.4 (line 152). While folding, two one-line amendments worth making: (a) spec §3.1's tuple parenthetical says `(code, url, impact, severity)` but `build_prevalence` passes a 5-tuple with category — the spec's own §3.1 text requires the stored category, so state the arity; (b) record spec §6's "hand-kept catalogue severity, third copy of one rule (P13)" as the TODO entry it says it is going to.
>
> No blocking findings. P5.4 is done. Proceed to the fold step, then `git push origin main`.

## Test evidence

| Surface | Command | Result |
|---|---|---|
| Python suite | `./venv/bin/python -m pytest tests/ -p no:cacheprovider --tb=short -q` | 5107 passed, 1 skipped, 2 deselected (integration), 184.24s, 21 warnings (pre-existing fpdf deprecations) |
| Targeted (cycle files) | `pytest tests/test_prevalence_agreement.py tests/test_prevalence.py` | 64 passed, 1.03s |
| Frontend | `npx vitest run` (frontend/) | 388 passed / 48 files, 5.29s |
| Lint (changed files) | `npx eslint <changed jsx/js>` | Vacuous — no frontend files in the range (4 files: crawl.py, prevalence.py, 2 test files). Python edits lint-clean per patch-tool syntax checks |
| Static scan | `git diff origin/main..HEAD` added lines grepped | Clean (no matches) |

Cycle tests: `TestRecalibrationCannotSplitTheTwoTables` (4.1, 4.2, 4.3 ×20 grid), `TestCodesTheCatalogueForgot` (4.4, 4.5, 4.5b stored category, 4.5c known-code stored severity), `TestMixedAndDescriptive` (4.6 ×2 seed orders, 4.7). The four P5.4 tests in `test_prevalence.py` use catalogue-derived fixtures — correct, since those stand in for a freshly-crawled job where stored and derived agree by construction; the disagreement cases live in the agreement file.

## Spec → code mapping (§3 changes)

| Spec change | Implementation | Tests |
|---|---|---|
| §3.1 stored impact/severity reach `compute_prevalence` | `build_prevalence` passes `(code, url, impact or 0, severity, category or "")` (prevalence.py:231-235); `Prevalence` gains `impact: int` (prevalence.py:44-48); row loop reads stored values (prevalence.py:121-122) | 4.5, 4.5c, 4.7 |
| §3.2 `_prevalence_for_display` filters on the stored impact | crawl.py:871-875 — `info_row_excluded(int(getattr(r, "impact", 0) or 0), level)`; `derive_impact` import removed; comment rewritten to record why | 4.1, 4.2, 4.3 |
| §3.3 catalogue-forgotten code keeps its row | prevalence.py:125-129 — `continue` only when `spec is not None and spec.scope == "site"`; labels fall back to code / stored category (prevalence.py:151-152) | 4.4, 4.5, 4.5b |
| §3.4 mixed stored impacts take the maximum | prevalence.py:118, 133-135 — `stored` dict, max-wins, deterministic | 4.6 (both seed orders) |
| Labels stay with the catalogue | prevalence.py:151-152 — description/category from `spec` when known | 4.7 |

## Mutation verification (independent, by the gate)

The gate applied the old filter back — `info_row_excluded(derive_impact(r.code), level)` with the import restored, the entire §3.2 change reverted — and re-ran the agreement file: **12 failed, 17 passed** (1.02s). Failures: 4.1, 4.2, and the discriminating 4.3 cells `[0|1|2|3]-{key|notable|none}` where the recalibrated value changes the predicate outcome (cells at `level="all"` and the stored-3/recal-0@notable-type agreements legitimately still pass — the predicate outcome is unchanged there, which is correct behaviour, not a hole). The 4.1/4.2 tests alone can be fooled in one direction each by a "drop everything"/"keep everything" hack — the spec says so — and the grid closes that. Mutation reverted; `git status` clean; 64 targeted tests green again.

The commit's six mutation claims are consistent with the adversarial constructions in the file: 4.5 guards `severity_from_impact(stored)` re-derivation (stored `info` at impact 5, where the derived label is `warning`); 4.5c guards the exact surviving variant the commit names (`severity=spec.severity if spec else severity`); 4.6 seeds both orders; 4.3 recalibrates away from the stored value at every level it can.

## Static scan

Added lines over `46eaf9e..b72b72d` scanned for hardcoded secrets, `os.system`/`shell=True`, `eval`/`exec`, `pickle`, SQL string-format injection. **Clean.** No new SQL is introduced; the store query layer is untouched.

## Findings

1. **(Fold-step) Spec §3.1 tuple arity is understated.** The spec's parenthetical says `build_prevalence` passes `(code, url, impact, severity)`; the implementation passes a 5-tuple with the stored category, which §3.1's own fallback text ("the category to the stored category") requires. Behaviour matches the spec; the parenthetical should name the 5th element when folded.
2. **(Fold-step) Spec §6's P13 note ("hand-kept catalogue severity … to TODO") has no TODO entry yet.** TODO.md's P5.4 item (line 152) is the item this gate reviews and is still open pending the fold; the §6 adjacency is a separate one-assertion TODO that should be recorded at the fold, as the spec promises.
3. **(Non-blocking) Stale type annotations in prevalence.py.** `count_indexable_pages` still declares `Iterable[tuple[str, str]]` (prevalence.py:73) and `compute_prevalence`'s docstring says rows are `(issue_code, page_url, impact, severity)` (prevalence.py:95) while callers pass 5-tuples. Both read only what they need (`row[0]`/`row[1]`, length-guarded elsewhere), so it is cosmetic drift, not a bug.
4. **(Non-blocking) crawl.py:875 double coercion.** `int(getattr(r, "impact", 0) or 0)` — `impact` is a typed `int` on the dataclass; the belt-and-braces mirrors the `getattr(r, "severity", None)` guard one line up and is harmless.
5. **(Observation, positive) Frontend vitest file count is 48, not the 47 the skill notes** — a test file added in an earlier cycle. No frontend change in this range; 388 green confirms no drift.

## State

- Branch `main`, clean tree. `origin/main..HEAD` = exactly the two P5.4 commits (spec first at 10:07, fix at 11:07 — micro-spec-before-code holds). Nothing else unpushed.
- Pre-fold state is expected and matches the P5.3 cycle: pending spec present, TODO P5.4 unchecked, not yet folded into `docs/functional-specification.md`. The disposal commit after this gate does: fold (with the two amendments above), delete pending file, check off TODO P5.4, record TODO P5.4b/§6 note, then push.
- No catalogue, scoring, thresholds, issueHelp.js, or issue-codes.md changes — this item touches no numeric bounds and no issue code, so `docs/thresholds.md` and the parity surfaces correctly stay untouched.
